services:
  db:
    image: postgres:16
    container_name: farm_db
    environment:
      POSTGRES_USER: farm
      POSTGRES_PASSWORD: farm
      POSTGRES_DB: farm_dev
    ports:
      - "5432:5432"
    volumes:
      - farm_postgres_data:/var/lib/postgresql/data
volumes:
  farm_postgres_data:
